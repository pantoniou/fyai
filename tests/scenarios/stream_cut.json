{
  "steps": [
    {
      "sse": [
        { "type": "response.created", "response": { "id": "resp_cut_1", "status": "in_progress" } },
        { "type": "response.output_text.delta", "delta": "half an ans" }
      ],
      "done": false
    }
  ]
}
