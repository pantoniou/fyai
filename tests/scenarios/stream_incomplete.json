{
  "steps": [
    {
      "sse": [
        { "type": "response.created", "response": { "id": "resp_inc_1", "status": "in_progress" } },
        { "type": "response.output_text.delta", "delta": "starting to answer" },
        { "type": "response.incomplete", "response": {
            "id": "resp_inc_1",
            "object": "response",
            "model": "mock-model",
            "status": "incomplete",
            "incomplete_details": { "reason": "max_output_tokens" }
          }
        }
      ],
      "done": true
    }
  ]
}
